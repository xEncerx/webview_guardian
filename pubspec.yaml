name: webview_guardian
description: "WebView client for Flutter with built-in adblock functionality."
version: 0.2.1
homepage: https://github.com/xEncerx/webview_guardian
repository: https://github.com/xEncerx/webview_guardian
issue_tracker: https://github.com/xEncerx/webview_guardian/issues

topics:
  - flutter
  - webview
  - inappwebview
  - adblock
  - privacy

environment:
  sdk: ^3.8.0

platforms:
  android:
  windows:

dependencies:
  crypto: ^3.0.7
  flutter:
    sdk: flutter
  flutter_inappwebview: ^6.2.0-beta.3
  meta: ^1.18.0
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  mocktail: ^1.0.5
  test: ^1.31.0
  very_good_analysis: ^10.2.0

flutter:
  assets:
    - assets/scriptlets.js
